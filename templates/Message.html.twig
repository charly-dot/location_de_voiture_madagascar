
{% for type, messages in app.flashes %}
    <div class="alert alert-{{type}}" role="alert" class="card p-4 box-shadown" style="text-align:center;">
        {{messages | join('. ')}}
    </div>
{% endfor %}