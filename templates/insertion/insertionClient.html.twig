{% extends 'base.html.twig' %}

{% block title %}Nouvel véhicule
{% endblock %}

{% block body %}

	<div class="example-wrapper card p-3 shadow " style="width:35%; margin-left:35%; margin-top:2%">
		{{ form_start(form) }}
		{{ form_widget(form) }}
		<div class="mt-3 d-flex justify-content-between">
			<button class="btn btn-info" type="submit">ENVOYER</button>
			<a class="btn btn-danger" href="{{ path('admin') }}">RETOUR</a>
		</div>
		{{ form_end(form) }}
	</div>
{% endblock %}
